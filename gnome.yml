---

- name: set gsettings
  become: false
  command: "gsettings set {{ item }}"
  with_items: "{{ gnome_settings }}"
  register: result
  failed_when:
    - result.rc != 0
  vars:
    cond: "{{ set_gsettings }}"
  when: cond